# Trader Bias Frontend Environment Configuration

# Backend API URL
VITE_BACKEND_API_URL=http://82.29.128.123:3001

# Use backend API for data fetching
# true = Fetch from backend (shared data, instant load for all users)
# false = Fetch directly from exchanges (client-side, per-user data)
VITE_USE_BACKEND=true
