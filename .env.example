# Trader Bias Frontend Environment Configuration

# Backend API URL (leave empty to use direct exchange APIs)
# For production: Set to your VPS backend URL (e.g., http://your-vps-ip:3001)
# For development: Use http://localhost:3001
VITE_BACKEND_API_URL=

# Use backend API for data fetching (true/false)
# true = Fetch from backend (shared data, instant load for all users)
# false = Fetch directly from exchanges (client-side, per-user data)
VITE_USE_BACKEND=false

# WebSocket connection for real-time updates (future feature)
# VITE_WS_URL=ws://localhost:3001
